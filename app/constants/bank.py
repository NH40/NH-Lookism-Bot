"""
Банковские константы — все числа патча 1.2.0 в одном месте.
Меняй здесь, не трогая логику сервисов.
"""

# ── Кредиты ───────────────────────────────────────────────────────────────────
# Максимальное количество одновременных кредитов у одного игрока
CREDIT_MAX: int = 3
# Коэффициент возврата: выплатить CREDIT_REPAY_FACTOR × сумму кредита
CREDIT_REPAY_FACTOR: float = 1.25
# Через сколько часов после просрочки блокируются действия (банда, атака, рейд)
CREDIT_BLOCK_HOURS: int = 3
# Через сколько часов после просрочки сносится банда
CREDIT_DELETE_HOURS: int = 6
# Максимальная сумма одного кредита: доход за CREDIT_INCOME_MINUTES минут
CREDIT_INCOME_MINUTES: int = 60

# ── Казино ────────────────────────────────────────────────────────────────────
# Шанс победы (%)
CASINO_WIN_CHANCE: int = 30
# Множитель выплаты при победе (ставка × CASINO_WIN_MULTIPLIER)
CASINO_WIN_MULTIPLIER: float = 2.0
# TTL истории ставок в Redis для детектора стратегии x3 (секунды)
CASINO_HISTORY_TTL: int = 3_600
# Граница детектора x3: ставка в диапазоне [ratio_min, ratio_max] от предыдущей считается x3
CASINO_X3_RATIO_MIN: float = 2.7
CASINO_X3_RATIO_MAX: float = 3.3

# ── Ячейки хранилища ──────────────────────────────────────────────────────────
# Максимальное число ячеек на игрока
STORAGE_MAX_SLOTS: int = 5
# Стоимость открытия одной ячейки (NHCoin)
STORAGE_OPEN_COST: int = 100_000
# Комиссия за хранение (NHCoin/минуту, только если ячейка не пуста)
STORAGE_FEE_PER_MINUTE: int = 200

# ── Инвестиции ────────────────────────────────────────────────────────────────
# Максимальное число одновременных вкладов
INVEST_MAX_SLOTS: int = 3
# Максимальная сумма одного вклада (NHCoin)
INVEST_MAX_DEPOSIT: int = 200_000_000
# Срок (часы) → доходность (%)
INVEST_DURATION_OPTIONS: dict[int, int] = {
    1:  3,
    3:  5,
    6:  10,
    12: 15,
    24: 20,
}
