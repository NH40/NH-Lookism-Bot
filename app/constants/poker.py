"""
PvP-покер (Texas Hold'em за один стол = одна раздача).
Меняй здесь, не трогая логику сервисов.
"""

# ── Стол ──────────────────────────────────────────────────────────────────────
POKER_MIN_PLAYERS: int = 2
POKER_MAX_PLAYERS: int = 6
# Границы суммы входа (NHCoin)
POKER_BUY_IN_MIN: int = 10_000
POKER_BUY_IN_MAX: int = 5_000_000
# Варианты времени ожидания игроков (секунды)
POKER_WAIT_OPTIONS_SECONDS: list[int] = [60, 180, 300]

# ── Блайнды ───────────────────────────────────────────────────────────────────
# big_blind = buy_in // POKER_BIG_BLIND_RATIO (~20BB эффективный стек)
POKER_BIG_BLIND_RATIO: int = 20
POKER_SMALL_BLIND_RATIO: int = 40

# ── Рейк ──────────────────────────────────────────────────────────────────────
# Невидимый рейк с банка (доля), уходит из экономии — никому не начисляется
POKER_RAKE_PERCENT: float = 0.05

# ── Тайминги ──────────────────────────────────────────────────────────────────
# Сколько секунд у игрока на действие, прежде чем сработает авто-фолд/авто-чек
POKER_ACTION_TIMEOUT_SECONDS: int = 30
